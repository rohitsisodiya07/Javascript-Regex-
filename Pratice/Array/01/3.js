//1. Convert array to object

    // let arr = [5, 10, 15, 20, 25] ;
    // let obj = arr.reduce( (acc, curr, index) => {

    //         acc[index] = curr ;
    //         return acc ;
    // }, {})
    // console.log(obj);
    
//2. Check if two objects are equal
    // let obj = { name : "Rohit", age : 22}
    // let another = { name : "Rohit", age : 21} ;

    // console.log( JSON.stringify(obj) === JSON.stringify(another));

//3.Find duplicate elements in array
    // let arr = [10, 20, 30, 40, 20, 40] ;
    // let result = arr.filter( (res, index) => arr.indexOf(res) !== index )
    // console.log(result);
    
//4. Merge two sorted arrays
    // let arr = [1, 3, 5, 7, 9] ;
    // let arr2 = [2, 4, 6, 8, 10] ;

    // let newArr = [...arr, ...arr2].sort( (a, b) => a - b) ;
    // console.log(newArr);
    
//5.