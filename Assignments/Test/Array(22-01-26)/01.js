// 1. Remove duplicate values from the given array.  

      let arr =  [1, 5, 3, 1, 5, 3, 1, 5, 3] 

      //Using Set

      // let newarr = [...new Set(arr)] ;
      // console.log(newarr);
      

      //Using Reduce
      // let newarr = arr.reduce( (acc, curr) => {

      //             if(!acc.includes(curr)){
      //                   acc.push(curr) ;
      //             }
      //             return acc ;
      // }, [] )
      // console.log(newarr);
      