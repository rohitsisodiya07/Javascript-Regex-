// Question: 1 Given an array of numbers, divide the array into smaller arrays of size 2.

    let arr = [ 1, 2, 3, 4, 5, 6, 7 ]
    let size = 2

    // let newarr = [] ;

    // for( let i = 0 ; i < arr.length ; i+=size){

    //         newarr.push( arr.slice(i, i+size))

    // }
    // console.log(newarr);
    

    //Using Reduce
    let newarr = arr.reduce( (acc, curr) => {

            

    }, [])
