// 32. Reverse a string using a loop.

    let name = "Rohit" ;
    let word = "" ;
    for(let i = name.length-1 ; i >= 0 ; i--){
           
        word = word + name[i] ;
    }
    console.log(word);
    