// Check if all elements in an array are numbers.

        let arr = [ 10, 20, 30, 40, 60, true] ;

       let check  = arr.every( num => (typeof num == "number"))

       console.log(check);
       
        
        