// Sort an array of numbers in ascending order.

    //sort

        let arr = [2, 5, 3, 1, 6, 7, 8, 4, 9] ;
        arr.sort( (a, b) => a-b )
        console.log(arr);
        
    //sort in descending

        // arr.sort( (a, b) => b-a) 
        // console.log(arr);
        

