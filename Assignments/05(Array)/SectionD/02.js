// 27. How does filter() work? Write a real-world example.  

// filter return all element that matches the condition and always return an array, return empty array[] if not present.

    let num = [10, 20, 30] ;
    let result = num.filter( a => a > 10) ;
    console.log(result);
    