// Convert an array of strings to uppercase using an array method.

    let arr = [ "Rohit", "Mohit", "mon", "ma"] ;

    //map
        let upper = arr.map( item => item.toUpperCase())
        console.log(upper);


    
    //String method.
        
    // let newArr = arr.toString().toUpperCase().split(",") ;
    // console.log(newArr);
    