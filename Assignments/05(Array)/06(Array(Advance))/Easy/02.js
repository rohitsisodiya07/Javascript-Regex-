// Find the maximum number in an array without using Math.max().

    let arr = [12, 50, 60, 40, 17, 96, 30, 41, 20, 22, 44] ;


//1. reduce

    // let maximum = arr.reduce( (acc, curr) => {

    //     return (acc > curr) ? acc : curr ;
    // })
    // console.log("Mac=ximum = " , maximum);
    