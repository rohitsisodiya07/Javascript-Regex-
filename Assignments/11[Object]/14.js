// 14. Convert an object keys into an array.

    let user = {
        name :"Rohit Sisodiya",
        age : 21,
        city : "Jaipur"
    }
    
    let arr = Object.keys(user)
    console.log(arr);
    